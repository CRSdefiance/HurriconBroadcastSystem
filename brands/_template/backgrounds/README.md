# Optional backgrounds

Add 16:9 PNG, JPEG, WEBP, or SVG files here. Reference them from `brand.json` as `assets.background` for the full scene and `assets.feedBackground` for the shared gameplay/camera fallback. Optional `feed1Background` through `feed4Background` and `runnerBackground` keys override individual slots.
